#!/usr/bin/env bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." >/dev/null 2>&1 && pwd )"
file="$DIR/plexwol.service"
sudo ln -s "$file" "/etc/systemd/system/plexwol.service"
sudo systemctl enable plexwol
sudo systemctl start plexwol
systemctl status plexwol
